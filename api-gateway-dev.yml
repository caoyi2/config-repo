zuul:
  routes:
    #/myProduct/product/list  ->  /product/product/list
    myProduct:
      path: /myProduct/**
      serviceId: product
      #sensitiveHeaders:
    #简洁的写法
    #product: /myProduct/**
  ignored-patterns:
    -/**/product/listForOrder

management:
  security:
    enabled: false
zuul:
  sensitive-headers:
